"workshopitem"
{
	"appid"		"2868840"
	"contentfolder"		"G:\\omp works\\sts2-regentfxfastboot\\workshop\\content"
	"previewfile"		"G:\\omp works\\sts2-regentfxfastboot\\workshop\\preview.png"
	"visibility"		"0"
	"title"		"RegentFX Fast Boot (万象辉星启动加速)"
	"description"		"[h1]RegentFX Fast Boot(万象辉星启动加速)[/h1]

[i]Conditional boot-stall fix for RegentFX. It requires a specific load order, it does not work at every mod position, and it never reorders your mods or writes your settings. When the launch was definitely NOT accelerated it says why, and when it was fully accelerated it says so - each as a one-shot popup on the main menu. A partly finished or partly failed warm-up reports nothing, so a silent launch is not a claim that nothing happened; the log has the details.[/i]

[hr]

[h2]它做什么(条件生效,顺序很重要)[/h2]

实测背景(2026-09-11, 39 个 mod 环境):RegentFX v0.5.1 在 mod 初始化阶段同步预加载 32 个特效场景,占用约 4 秒(2026-09-15 复核:历史计时区间包含程序集/PCK/初始化的全部工作,不止预加载本身)。

本 mod 只在满足前置顺序时生效:
[list]
[*][b]硬性顺序要求[/b]:RegentFXFastBoot 必须排在 RegentFX [b]上面[/b].顺序的载体是 settings.save 里的 mod_list,而游戏界面对它只能勾选启用/禁用,不能改顺序,因此必须用调序工具(见下方"使用方法").顺序不满足时,日志会如实报告 LATE-ORDER:本次启动不跳过任何内容,不做预热,启动卡顿(如发生)已经发生且无法撤回.
[*]顺序正确时:在 RegentFX 的程序集加载瞬间挂上方法绑定,等它的初始化器真正调用同步预加载入口时才拦截(此时 PCK 挂载、脚本注册、RegentFX 配置默认值都已完成)
[*]仅当 RegentFX 自己的 PreloadEffects 开关为开启时才会发生拦截;该开关关闭时本 mod 不做任何预热,保持原生关闭预加载的策略
[*]拦截后把相同的场景清单交给引擎自己的异步加载面(NAssetLoader/AssetLoadingSession)预热进 RegentFX 自己的缓存:同一时刻最多一个可选请求,只在主菜单空闲时提交,不抢占引擎必需资源的加载带宽;离开主菜单后不再提交新请求,已在途的请求观察到完成
[*]预热失败或未完成的场景由 RegentFX 原生懒加载路径兜底(VFXUtil.GenVFXNode 回落 PreloadManager.Cache),特效不会因此缺失;但这是回落路径,不是[b]永不失败[/b]的保证
[/list]

[h2]关于进战斗前缓存是否预热完成[/h2]

[list]
[*]第一场战斗前缓存是否已热[b]取决于时机[/b](机器速度、帧率、启动流程),本 mod 不对此作出保证
[*]日志会按阶段如实报告:ARMED / BOUND / INTERCEPTED / QUEUED / ATTACHED / COMPLETED / LATE-ORDER / FAILED / UNSUPPORTED,可据此验证模组顺序调整是否生效
[/list]

[h2]使用方法[/h2]

[list]
[*]订阅本 mod 与 [url=https://steamcommunity.com/sharedfiles/filedetails/?id=3747497501]RegentFX[/url]
[*][b]关键步骤:设置加载顺序.[/b]游戏本体的模组界面[b]没有[/b]调整顺序的功能(只有启用/禁用勾选),所以顺序必须由调序工具写入.推荐 [url=https://steamcommunity.com/sharedfiles/filedetails/?id=3747605109]Load Order Manager[/url]:装好后在模组界面点"加载顺序",把 RegentFXFastBoot 移到 RegentFX [b]上面[/b],点"应用",重启游戏生效.
[*][b]另请确认本 mod 只装了一份.[/b]本地副本与工坊副本同时存在时,引擎会在列表尾部写回第二行同名记录;排序只按 id 取[b]最后一行[/b]的位置,那一行会覆盖启用行的位置,导致每次启动都晚序.
[*]未安装 RegentFX 时本 mod 保持待命,不做任何事
[*][b]加速成功时的弹窗[/b]:若本次启动确实被加速(全部场景提交完成、至少预热了一个、无失败),进入主菜单后会弹一次提示窗,告知预热了多少个场景以及启动卡顿已消失;只有一个“知道了”按钮,无需任何操作。部分完成或有失败时不会弹此窗,避免夸大实际效果。
[*][b]加速失败时的弹窗[/b]:若本次启动无法加速(RegentFX 先加载,同步预加载已经发生且无法撤回),进入主菜单后会弹一次提示窗,说明上面这四步,并提供两个按钮:[b]使生效[/b](打开 Load Order Manager 的工坊页面并显示步骤)与[b]不再提示[/b](记住选择,之后不再弹)。弹窗不写任何设置,不重排任何 mod;选"不再提示"后加速功能保持待命,之后若顺序修好仍会自动生效。
[/list]

[hr]

[h2]依赖[/h2]

[list]
[*][b]配合[/b]:[url=https://steamcommunity.com/sharedfiles/filedetails/?id=3747497501]RegentFX[/url](未安装则本 mod 无事可做)
[*]v0.2.0 起不再依赖 BaseLib(经二进制核查,本 mod 的 DLL 对 BaseLib 的运行时引用为零)
[/list]"
	"changenote"		"v0.4.0: the mod now tells you what happened, once, on the main menu. Previously it was silent in both directions. Subscribers jump straight from 0.2.0, so both popups are new here. (1) If the launch WAS accelerated - the whole scene queue submitted, at least one scene warmed, none failed - a one-shot popup reports how many scenes were warmed and that the boot stall from RegentFX's synchronous preload is gone. One button, no action needed. (2) If the launch could NOT be accelerated (RegentFX loaded first, so its synchronous preload had already run and cannot be undone), a one-shot popup explains why and how to fix it, with 'apply' (opens the Load Order Manager Workshop page and shows the steps) and 'do not show again'. A partially completed or partly failed warm-up stays silent on purpose: the popup never claims a saving that did not happen. Each notice has its own one-shot flag in the mod's state file under the game's user data directory, so seeing one never suppresses the other; neither writes your game settings - the only file the mod writes is its own notice state - and neither reorders your mods. Also carried over from 0.2.1: the corrected install instructions (the vanilla modding screen has no reorder control; use Load Order Manager, Workshop 3747605109) and the single-install rule."
	"publishedfileid"		"3799305611"
}
